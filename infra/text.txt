Here docker implementation can go? We can change it later as we like. 
perhaps db stores, object stores, load balancers, connection to s3, postgres, etc. monitoring stuff(Prometheus,google analytics,etc.,alarms), etc.